import styled from 'styled-components';

export const Wrapper = styled.section`
  padding: 3rem 1rem;
  background: rgba(0, 0, 0, 0.8);
  color: #fff;
  text-align: center;

  a + a {
    margin-left: 1rem;
  }

  img {
    width: 2rem;
  }

  small {
    font-size: 0.65rem;
  }
`;
